
services:
  api:
    build:
      context: .

    image: task-api:compose

    container_name: task-api-compose

    ports:
      - "8000:8000"

    environment:
      DATABASE_PATH: /app/data/tasks.db

    volumes:
      - task_data:/app/data

    restart: unless-stopped


volumes:
  task_data: